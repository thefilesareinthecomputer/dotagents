# Fixture repo

A tiny polyglot codebase for exercising code-kg.

The entry point is [app/main.py](app/main.py); zones are summarized by
[the util module](app/util.py).

```bash
# Fenced examples never become edges:
python3 app/orphan.py
```
